import React from 'react';

function Button(props) {
    return (
        <div>
            <button>
                click me!
            </button>
            
        </div>
    );
}

export default Button;